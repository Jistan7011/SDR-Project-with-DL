진짜 사이버 전자전에서 적군의 각종 신호 분석에 써먹을려면 어느정도의 성능 및 어떤 시스템을 만족해야하나
오전 11:12


실전 “전자전에서 적 신호 분석” 수준으로 쓰려면, 지금 프로젝트의 BASK/BFSK/BPSK 3-class 분류와는 요구 수준이 완전히 다릅니다. 현재 실험은 **학부 연구/프로토타입 단계의 AMC(Automatic Modulation Classification)**이고, 실전급은 광대역 신호 탐지 + 분리 + 식별 + 추적 + 복원 + 신뢰도 판단 + 운용자 의사결정 지원 시스템이어야 합니다.

법적/윤리적으로 실제 군사·공공망·타인 통신을 무단 수집/분석/방해하는 건 문제가 될 수 있으니, 아래는 연구실/모의 환경 기준의 기술 요구사항으로 설명하겠습니다.

현재 프로젝트 위치
현재 수준:

단일 송신원
단일 변조
BASK / BFSK / BPSK 3-class
433 MHz 근거리 OTA
HackRF TX + RTL-SDR RX
window 기반 offline classification
accuracy 약 0.75~0.82
payload 복원은 아직 본격 end-to-end 검증 전
즉 지금은 “실전 신호 분석기”가 아니라:

SDR 기반 변조 분류 연구 프로토타입
입니다.

실전급에 가까워지려면 필요한 성능
최소 연구용 목표:

Known signal modulation classification accuracy: 90% 이상
Unknown/open-set rejection 가능
SNR -5~20 dB 범위별 성능 보고
다중 session / 다중 거리 / 다중 장비 generalization
실시간 또는 near-real-time 처리
실전급에 가까운 목표:

광대역 spectrum scan 가능
신호 존재 탐지 Pd 높음, false alarm 낮음
동시 신호 분리 가능
unknown signal cluster/labeling 가능
변조뿐 아니라 protocol/fingerprint 추정 가능
신호별 confidence와 uncertainty 제공
운용 환경 변화에도 성능 유지
단순 accuracy 하나로는 부족합니다. 최소한 아래를 봐야 합니다.

Detection probability
False alarm rate
Modulation classification accuracy
Open-set unknown detection rate
SNR별 성능
거리/방향/이동/다중경로별 성능
Signal separation success rate
Payload/frame recovery rate
Latency
Throughput
Confidence calibration
필요한 시스템 구성
실전형 신호 분석 파이프라인은 보통 이렇게 갑니다.

Wideband IQ capture
→ spectrum sensing
→ signal detection
→ channelization
→ burst detection
→ signal separation
→ RF impairment correction
→ feature extraction / deep model
→ modulation classification
→ protocol/frame analysis
→ demodulation
→ payload/error analysis
→ emitter fingerprinting
→ tracking / logging / visualization
현재 프로젝트는 이 중에서:

channelized IQ window
→ modulation classification
일부만 한 상태입니다.

앞으로 필요한 핵심 기능
신호 탐지

지금은 “언제 어떤 신호가 있는지”를 알고 capture합니다. 실전형은 모릅니다.

필요 기능:

noise floor estimation
energy detection
burst detection
occupied bandwidth estimation
center frequency estimation
signal start/end detection
다중 신호 처리

지금은 한 번에 하나의 신호만 보냈습니다. 실전형은 여러 신호가 동시에 있습니다.

필요 기능:

adjacent-channel separation
overlapping signal detection
channelizer/filter bank
time-frequency segmentation
signal clustering
Open-set 인식

지금 모델은 무조건 BASK/BFSK/BPSK 중 하나로 찍습니다. 실전에서는 모르는 신호가 훨씬 많습니다.

필요 기능:

known / unknown 판별
confidence threshold
out-of-distribution detection
embedding clustering
RF impairment 보정

실전 신호는 깨끗하지 않습니다.

필요 보정:

carrier frequency offset
timing offset
phase offset
sample-rate offset
IQ imbalance
DC offset
multipath fading
AGC/gain variation
데이터 복원

변조 분류만으로는 부족합니다. 실제 목적은 신호의 의미를 파악하는 것입니다.

필요 기능:

demodulation
symbol timing recovery
frame synchronization
CRC/checksum validation
BER/CER/packet success rate
protocol inference
실시간성

offline .bin 분석만으로는 부족합니다.

필요 목표:

streaming IQ input
sliding-window inference
GPU/CPU latency 측정
windows per second 측정
real-time dashboard
alert/logging
현재 프로젝트 기준 다음 단계
가장 타당한 순서는 이겁니다.

실험 4:
  변조 분류 + DSP 복조 + payload 복원

실험 5:
  SNR/AWGN 및 실제 거리 변화 stress test

실험 6:
  인접 채널 간섭 / 다중 신호 실험

실험 7:
  unknown signal / open-set rejection

실험 8:
  실시간 수신 파이프라인
지금 당장 “전자전급”을 목표로 한다면, 실험 4가 제일 중요합니다.
왜냐하면 실전에서는:

분류만 맞음
보다

신호를 탐지하고, 변조를 식별하고, 데이터를 복원하고, 신뢰도까지 제시함
이 필요하기 때문입니다.

한 줄 판단
현재 시스템은 실전 전자전 분석기가 아니라 SDR 기반 변조 분류 연구 프로토타입입니다.

실전형에 가까워지려면 최소한:

다중 신호 탐지
open-set 분류
RF impairment 보정
payload 복원
실시간 처리
신뢰도/불확실성 출력
조건별 성능 검증
까지 가야 합니다. 지금 다음 단계는 실험 4: end-to-end 데이터 복원이 맞습니다.